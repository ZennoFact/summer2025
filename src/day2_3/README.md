# Day2-3

## 改造開始

初日に作った Figureクラス，CircleクラスとRectクラスを改造して，座標等のデータを管理してアニメーションさせるようにしましょう。

## 大事なこと

1. コンポーネントの`paint`メソッドを呼ぶと`paintComponent`が発動する。
2. つまり，定期的にpaintメソッドを呼ぶためのタイマーが必要。今回はTimerクラスのインスタンスを使って実施。